<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="vtu.hackathon.srrs.futureeducation" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".AppController"
        android:allowBackup="true"
        android:icon="@mipmap/icon"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.AppTutorialScreen"
            android:label="App Tutorial"
            android:screenOrientation="portrait"
            android:theme="@android:style/Theme.Black.NoTitleBar.Fullscreen" />
        <activity
            android:name=".activity.MainStreamActvity"
            android:label="@string/title_activity_main_stream_actvity" >
        </activity>
        <activity
            android:name=".activity.SubStreamActivity"
            android:label="@string/title_activity_sub_stream" >
        </activity>
        <activity
            android:name=".activity.EachStreamActivity"
            android:label="@string/title_activity_each_stream" >
        </activity>
    </application>

</manifest>
